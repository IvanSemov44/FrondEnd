import { Box, Typography } from "@mui/material"

const LoginPage = () => {
    return (
        <Box padding={4}>
            <Typography>Login Page</Typography>
        </Box>
    )
}

export default LoginPage